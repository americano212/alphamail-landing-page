import React from 'react';
import ReactDOM from 'react-dom/client';
import './submit.css';
import Submit from './jsx/Submit';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Submit />
  </React.StrictMode>
);